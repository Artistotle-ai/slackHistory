{
  "name": "mnemosyne-oauth-callback",
  "version": "1.0.0",
  "description": "Slack OAuth callback Lambda function for Mnemosyne",
  "main": "dist/index.js",
  "scripts": {
    "build": "esbuild src/index.ts --bundle --platform=node --target=node20 --format=cjs --outfile=dist/index.js --external:@aws-sdk/* --external:aws-lambda --minify --tree-shaking=true --analyze --banner:js='globalThis.require = require;'",
    "build:dev": "tsc",
    "test": "jest --passWithNoTests",
    "watch": "tsc -w"
  },
  "dependencies": {
    "@aws-sdk/client-dynamodb": "^3.0.0",
    "@aws-sdk/client-secrets-manager": "^3.0.0",
    "@aws-sdk/lib-dynamodb": "^3.0.0",
    "mnemosyne-slack-shared": "file:../slack-shared"
  },
  "devDependencies": {
    "@types/jest": "^29.5.12",
    "@types/node": "^22.0.0",
    "esbuild": "^0.19.12",
    "jest": "^29.7.0",
    "ts-jest": "^29.2.5",
    "typescript": "^5.9.0"
  }
}
